{
  "name": "hockey-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
  },
  "cacheDirectories": [
    "node_modules",
    "server/node_modules"
  ],
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "express": "^4.16.3",
    "fetch": "^1.1.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4",
    "request": "^2.85.0",
    "ws": "^5.1.1"
  },
  "scripts": {
    "start": "node server && react-scripts start",
    "heroku-postbuild" : "npm install && npm install --only=dev --nos-shrinkwrap && npm run build",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
